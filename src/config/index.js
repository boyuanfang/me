export const srconfig = (delay = 200) => ({
  // animation
  origin: 'bottom',
  distance: '12px',
  duration: 500,
  delay,
  easing: 'cubic-bezier(0.445, 0.05, 0.55, 0.95)',
  // config
  viewFactor: 0.5,
  reset: false,
});

export const name = 'sb';
